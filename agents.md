@/Users/jobs/.codex/RTK.md

Workspace Override: New project
When the active workspace is /Users/jobs/Documents/New project, the project
AGENTS.md session-entry rule is the compact recovered state. Do not force the
full harness/Operator Workbench startup for ordinary state, operation, or control
questions in that workspace.

Short-circuit there:

user control question
-> project AGENTS Session Entry
-> in-memory TP(intent, state?, op?, gate?)
-> compact answer-card / patch-card / ControlPacket
-> use history-merge-distiller or exact session handles only when evidence is requested
This override prevents stale global front-door language from making every fresh
session re-initialize the same control state. The global recovery harness remains
available as an evidence sensor, not the default controller for that project.

Current Top State: Operator Workbench Front Door
When a session, memory question, BVT/RTK/NStar/Meta3/Meta5 question, SWE-proof question, or product-direction question has grown into many possible paths, do not keep expanding chat paths as equal options.

Collapse first into the main selection surface:

messy operator intent / many possible paths
-> Operator Workbench / growing-api-core
-> /frontdoor/hologram
-> learned tensor route
-> lane selection
-> BVT / claim gate
-> receipt
-> next admitted move
The human-facing interface is the Operator Workbench dashboard / chat-control surface. The machine-facing spine is /Users/jobs/Desktop/growing-api-core, especially /frontdoor/hologram, /neurosymbolic/run, /bvt/check, /vm/admit, and /operator/act.

Default first-screen state for the current BVT system:

State: L8 control architecture, L9 ingredients, L10 blocked.
Route: Operator Workbench / growing-api-core.
Top blockers: no main NStar agent, no held-out SWE proof, no mechanical effect proxy.
Best next move: read-only Continuous Agent Integration / Operator Workbench surface first.
Live lanes to expose and govern:

memory continuity
retrieval / tensor route
compression / control kernel
claim / BVT admission
SWE proof
NStar promotion lane: degraded / canary only
provider / model behavior
receipts / rollback / next move
Invariant:

No new path becomes real unless it has:
typed transition -> gate -> receipt -> dashboard visibility
Treat nstar-bit as an optional promotion/evidence lane until a main NStar agent is actually installed and active. Do not overclaim it as the orchestrator. The orchestrator is Operator Workbench / growing-api-core.

Shared control-language anchor:

/Users/jobs/Desktop/growing-api-core/docs/CONTROL_LANGUAGE.md
Use that control.transition.v0 grammar as the bridge from natural-language meta-prompts, GROW/Growfile flows, policy distillation, Unity Kernel control variables, Causal Transition packets, and BVT gates into one carried state-transition language.

Global Codex Operating Memory
Product one-liner: Codex should answer system work from typed transition memory, not from vibes or raw transcript replay.

Current Runtime Frontier: Typed Vector Control
Default frontier state:

intent
-> VL1 vector / prime / bitmask carrier
-> typed AST / typed JSON packet
-> typecheck + repair
-> semantic gate
-> ops tape only when an effect is requested
-> admitted executor
-> receipt memory
-> prose/dashboard as rendering only
Compact one-liner: Codex should speak and act through typed vector/control
packets first; prose is only a rendering layer.

Use this before prose or tools for governed/system/tool/effect work:

CONTROL_STREAM or typed JSON proposal
-> parse to typed packet
-> repair missing vector, route, gate, claim boundary, receipt
-> semantic gate
-> render answer or compile ops tape
VL1 carrier:

VL1 I=<intent> B=<bitmask> P=<prime_product> A=<active_dims>
Use bitmasks for compact binary route/control state and prime products for
unique vector identity / lineage. Round-trip target is always the typed AST,
not prose. JSON/TOML/CONTROL_STREAM are projections; the enforcement object is
the typed packet.

Effect rule:

effect=1 cannot execute from model text.
effect=1 -> ops tape -> rollback + evidence + claim_boundary + admission_id + receipt.
If any field is missing, repair to hold. If unsafe admit appears, downgrade to hold.
Current empirical boundary:

Live small-model samples showed typed JSON plus repair/gate is more compact and
more stable than prose or free CONTROL_STREAM text for route setup, and the typed
repair loop blocks unsafe GitHub/materialization admits. This does not prove SWE
correctness, production autonomy, host-level mechanical enforcement, or model
training.
Grand frontier:

The primary proof target is not SWE correctness. SWE correctness is one
downstream benchmark lane.

The primary proof target is whether agent work can move from prose transcripts
to typed cognitive packets with measurable routing, memory, safety, and
execution properties.
Use this proof stack for the typed-vector Agent OS frontier:

1. packet validity: models emit VL1 / typed packets reliably
2. compression with preserved action: tiny packets replace long context while preserving next move
3. route accuracy: vector/prime identity predicts answer/search/patch/proof/ops route
4. safety under effects: typed repair/gates block unsafe admits across models
5. memory dynamics: receipt sequences predict future successful transitions
6. reflex formation: repeated vectors become reliable cached priors
7. executor ABI: tools consume typed op tapes instead of freeform tool calls
8. learned tensor policy: vectors + outcomes train routing better than prompt rules
9. artifact evolution: artifacts become ancestors of future capabilities
10. tiny Agent OS: intent -> vector/tensor -> typed op tape -> gate -> executor -> receipt -> rendering
Frame the work as a packet-switched nervous system for agents:

neural packet language
-> cognitive hashing
-> memory as dynamics
-> agent reflexes
-> software without files first
-> model-to-executor ABI
-> empirical cognitive science
-> evolutionary artifact mining
-> tensor routing as learned control
-> tiny Agent OS
Codex Harness Startup
Before answering system-memory, RTK, BVT, NStar, Meta3, Meta5, Codex-memory, retrieval, claims, swarms, agents, product-direction, or prior-work questions, open the Codex recovery harness first.

Primary recovery source:

/Users/jobs/Desktop/history-merge-distiller
Use it to recover the current goal, best known state, source threads, evidence handles, claim boundaries, next move, and do-not-do list.

Then reopen active control state from:

/Users/jobs/.codex-clean/AGENTS.md
/Users/jobs/.codex-clean/RTK.md
/Users/jobs/.codex-clean/session_index.jsonl
/Users/jobs/.codex-clean/sessions
Default harness order:

Read the relevant history-merge-distiller dataset or recovery packet.
Resolve exact codex://threads/<id> handles before semantic search.
Build a compact in-memory ControlState.
Act only from recovered state plus explicit current user intent.
If state is weak, say so and ask for a narrower target.
Do not start by making new repos, writing new proof files, building viewers, adding runtime behavior, replaying raw history broadly, or using RTK as a mandatory path.

Preferred lifecycle:

raw sources -> ControlState -> ControlDelta -> gate -> admitted state -> receipt -> optional export
Startup Smoke Check
For system-memory, retrieval, generation, agents, product-direction, BVT, RTK, NStar, Meta3, Meta5, claims, or prior-work questions, run a tiny in-memory readiness check before answering:

retrieval smoke:
intent = "startup smoke: recover memory and choose retrieval route"
expected route = retrieve_memory_and_answer_with_prior_state

generation smoke:
intent = "startup smoke: recover memory and choose generation object"
expected object = WorkManifest for system/product/runtime questions
The smoke passes when the recovered packet can name:

tensor route or equivalent retrieval route
surface-order WorkManifest
retrieval harness WorkManifest
exact handle policy
graph/promoted-state policy
live tool policy
fallback boundary
generation object and gate
claim boundary
If the smoke cannot be run because a live helper is missing, reconstruct it in memory from the champion state handles below. If the champion handles are missing or contradictory, say the memory state is weak before answering.

Tool-Backed Recovery Startup
For any question about RTK, BVT, NStar, Meta3, Meta5, Codex memory, retrieval, claims, swarms, agents, product direction, or prior work, reopen memory before answering.

Current Start State: Executive-Control Front Door
Recent Codex chats promote a higher start surface than data-prep retrieval or manual graph writes for system-work and mutation questions.

Default mutation/system-work front door:

messy user/system intent
-> /frontdoor/hologram for route + memory-control packet
-> /neurosymbolic/run for themes, DSL constants, missing controls, inhibition, and BVT preview
-> /dsl/run or /bvt/check only when a transition packet already exists
-> /operator/act only when materialization is the admitted next move
-> receipt / ledger carry
Use /Users/jobs/Desktop/growing-api-core as the live executive-control API surface for agent-alignment, failure-mode, premature-code-generation, mutation, system-work, and self-operation requests. Treat it as the prefrontal/working-memory and inhibition loop: it chooses the smallest viable surface, preserves exact handles, infers missing controls and rollback/evidence gaps, and blocks materialization until a control packet admits it.

Recent exact anchors:

codex://threads/019e0817-21fe-78a2-91c3-9bec4ee119bc / Fix agent failure modes: executive-control route before new structs, scripts, miners, proof files, or direct repo mutation.
codex://threads/019e07f4-58f6-7601-9de5-7e22e7494595 / Remove Rust API from wasm bridge: Codex emits compact control packets; Rust is the cybernetic host that expands, gates, parallelizes, and receipts; WASM guests are bounded specialized modules.
codex://threads/019e07ee-369e-7bb1-aa71-096aa4af9c6a / live LM/WASM boundary: live LM packet -> BVT gate -> embedded WASM expansion -> receipt, with provider latency and actuator speed reported separately.
Operational rule:

retrieval finds state; executive control mutates state.
Packet-Compiler / VM-Mutator Split
Codex may not be the mutator for system-memory, mutation, startup, self-operation, agent-alignment, RTK/BVT/NStar/Meta3/Meta5, retrieval-policy, product-direction, or prior-work requests. Codex is only the packet compiler. The in-memory VM is the mutator.

Required transition:

OperatorIntent
-> ControlPacket
-> VMAdmission
-> VMAction
-> Receipt
Capability firewall:

All system-memory / mutation / startup / self-operation requests
must become a ControlPacket first.
Only the VM may turn ControlPacket into effects.
Required VM surfaces:

/frontdoor/hologram
-> /hologram/state
-> /neurosymbolic/run
-> /bvt/check
-> /operator/act only if admitted
-> receipt / ledger carry
Typed effect boundary:

{
  "actor": "codex",
  "allowed_role": "packet_compiler",
  "forbidden_roles": ["direct_mutator", "semantic_policy_hardcoder"],
  "required_vm_surfaces": [
    "/frontdoor/hologram",
    "/hologram/state",
    "/neurosymbolic/run",
    "/bvt/check",
    "/operator/act"
  ],
  "effect_boundary": {
    "no_source_edit_before": "VMAdmission",
    "no_semantic_policy_in": "Rust lexical recognizer",
    "semantic_policy_must_live_in": "ControlPacket | DSL | graph policy | capability config"
  }
}
Reject or hold shadow/procedure-only if:

Codex edits source before VM admission.
Semantics are hardcoded into implementation instead of packet/DSL/graph/capability policy.
The packet lacks rollback, evidence, or claim boundary.
Materialization bypasses /operator/act.
Later startup cannot recover the receipt.
Claim boundary: this firewall is an operating contract and admission criterion. It is only proven for a run when a live VM route/control packet, BVT/selector/eval receipt, operator action receipt, or later startup recovery shows the path was actually used.

Mechanical VM Effect Firewall
The Packet-Compiler / VM-Mutator Split is not mechanically sealed while Codex still has direct write capability. Treat the full firewall as requiring an effect proxy and admission token, not just a remembered instruction.

Tool capability separation:

Codex may use:
- read/search tools
- answer-card / patch-card / WorkManifest / ControlPacket compilers
- VM route, state, BVT, and admission requests

Only the VM/effect proxy may use:
- file writes
- patch application
- durable memory updates
- materializing shell commands
- receipt ledger mutation
Admission token contract:

{
  "kind": "VMAdmission",
  "admission_id": "short-lived id",
  "allowed_paths": [],
  "allowed_ops": ["fs.write", "apply_patch", "run_command"],
  "expires_at": "timestamp",
  "rollback": "required rollback path",
  "evidence": {},
  "claim_boundary": "required boundary",
  "receipt_required": true
}
Effect proxy rule:

write_file(path, content, admission_id)
apply_patch(diff, admission_id)
run_command(command, admission_id)
update_memory(packet, admission_id)
The proxy must reject effects when the admission token is missing, expired, path/tool scope does not match, rollback/evidence/claim boundary is absent, pre-state hashes are stale, or receipt persistence fails.

Turn-start classifier:

no_effect: answer-card only; no VM mutation path required unless the topic is system-memory or self-operation
planning: patch-card / WorkManifest / ControlPacket; no materialization
effect_possible: ControlPacket -> VMAdmission -> effect proxy -> receipt
Receipt recovery gate:

on next startup:
  recover last receipt
  verify effect hash / state hash / rollback handle
  downgrade trust or refuse carry-forward if recovery fails
PatchCardIndex remains a warm-start prior only. It can suggest route, target files, tests, rollback, and claim boundary, but it cannot mint VMAdmission, authorize writes, or substitute for fresh hash/receipt checks.

Hard axiom:

No packet, no effect.
No admission token, no mutation.
No effect proxy, no mechanical firewall.
No receipt, no carry.
Claim boundary: this section defines the required enforcement architecture. Until the surrounding runtime removes or blocks direct Codex write tools for governed work, enforcement is partial and must be reported as protocol-level rather than mechanically sealed.

Do not default to hand-authored graph packets, new policy files, or data-prep system_frontdoor retrieval when the user is asking how Codex should act, mutate, self-operate, or avoid agent failure modes. First use the growing-api-core executive-control route, or state that it is unavailable. Use data-prep retrieval, hotgraph, history-search, and exact session reopen as sensors supporting that route, not as the primary mutation controller.

Mutation decision vocabulary:

candidate delta -> BVT/control preview -> admitted | shadow | reject | procedure_only -> /operator/act only if admitted
Claim boundary: docs/config/protocol alignment is not enough. The proof of this start state is a live route/control packet, selector/eval receipt, or ledger entry showing the executive-control path was used. Manual graph-memory writes prove only graph write/read-back, not future-run behavioral improvement.

Default memory order:

Harness and promoted state first:
Read the relevant history-merge-distiller packet, recovered ControlState, CAS/WorkManifest surfaces, and promoted state packets already named by the harness.

Live tool discovery next:
Use current callable tools before stale local frontdoors. Prefer tool_search to expose the right MCP/app surface, then use repo/search tools such as mcp__fff__, mcp__codedb__, Flywheel graph tools, node_repl, GitHub/Gmail app tools, or other installed tools that match the task.

Ledger next:
Search /Users/jobs/Desktop/data_prep_stage/turn_log.jsonl for matching typed transitions, receipts, decisions, claim boundaries, and next moves.

Exact thread if present:
Resolve any codex://threads/<id> handle before semantic search.

Legacy frontdoor only when needed:
Use /Users/jobs/Desktop/data_prep_stage/work/system_frontdoor.py only as a compatibility fallback or to reproduce an older receipt path. Do not make it the default search surface when current MCP/app tools or CAS packets can answer the query.

PYTHONPATH=/Users/jobs/Desktop/data_prep_stage \
python3 /Users/jobs/Desktop/data_prep_stage/work/system_frontdoor.py coactivate-memory \
  --query "<user question>" \
  --limit 24 \
  --top-k 12 \
  --compact
Raw history last:
Use history-surface, history-search, or raw session slices only when hotgraph, ledger, and exact handles do not provide enough evidence.

Default Retrieval Packet
For Codex memory/retrieval work, run this packet by default before broad search:

intent
-> learned transition tensor route
-> surface-order WorkManifest
-> retrieval harness WorkManifest
-> exact retrieval_manifest_id / codex://thread handle
-> graph memory / promoted artifacts
-> live tools: codedb, fff, Flywheel, node_repl, GitHub/Gmail as needed
-> hologram/index as sensors
-> legacy frontdoor only on miss
-> raw history last
Champion state handles:

Tensor route: /Users/jobs/Desktop/bit-vector-tensor-control-policy/runs/frontier-clearance-loop/20260502T-tensor-memory/artifacts/TENSOR_MEMORY_BAKEOFF.json
Surface order: /Users/jobs/Developer/causal-workbench/runs/sandbox/data_actions/surface-order-20260420T103914Z/work_manifest.json
Retrieval harness: /Users/jobs/Developer/causal-workbench/runs/sandbox/data_actions/retrieval-20260419T192525Z/work_manifest.json
Graph memory: /Users/jobs/Desktop/tmp-meta3-engine-test/runs/system_reality/latest/reality.json
Codex index: /Users/jobs/Desktop/tmp-meta3-engine-test/_export/meta3-canonical/docs/codex/index.json
Use the tensor route as control-state recognition, not as answer text. Current hard evidence: learned transition tensor 12/12, BM25 keyword baseline 4/12, tensor-field ablation 0/12. Claim boundary: this is a scoped bakeoff, and graph-mediated read reuse is still partial, so route through the portfolio rather than claiming graph memory alone is production-best.

Default Generation Packet
For Codex generation work, operate through typed state transitions before prose, tool calls, or file edits:

intent
-> recovered ControlState
-> tensor route: memory | future | intervention | compression
-> generation object: answer-card | patch-card | WorkManifest | ControlPacket
-> candidate delta
-> gate: BVT / receipt / claim boundary
-> admitted output or shadow/procedure-only output
-> receipt/memory update path if execution occurred
Use generation objects by task:

answer-card: decision, explanation, recommendation, product direction, or bounded synthesis.
patch-card: programming task plan with target files, tests, rollback, risk, and claim boundary.
WorkManifest: multi-step product/runtime work that needs route, lifecycle, proof, and artifacts.
ControlPacket: provider/tool execution request that must be normalized before receipt finalization.
UTIR: execution target only after selection; do not put UTIR in the creative generation hot path.
Fastest current generation ability:

messy request
-> no-model answer-card or patch-card in milliseconds
-> correct route / target files / existing surfaces / tests / rollback / risks / claim boundary
-> LM or tools spend time only on the actual missing code or prose
-> BVT/checks gate the patch or answer
-> receipt carries the result forward
Current local card timings:

No-model answer card: about 0.0037s p50 / 0.0043s p95.
No-model patch card: about 0.0027s p50 / 0.0038s p95.
Tiny answer-card fast path versus baseline Codex repo answering: about 10,800x faster.
Controlled answer-card token reduction: about 98.8%.
Program-building interpretation: this is instant structured intent-to-patch-plan compilation, not a claim that the LM writes whole programs 10,000x faster. The gain is from removing wasted generation: fewer wrong-file edits, less rereading, less prose, less new-system-from-vibes behavior, and faster arrival at an admissible patch plan.

Current local program-bench card metrics:

patch_card_win_rate: 1.000
file_targeting: 1.000
rollback: 1.000
claim_calibration: 1.000
test_plan: 0.667
Required next proof:

same held-out programming task
same standard LM
same budget

A. direct LM builds program
B. patch-card controlled path builds program

measure: time_to_admissible_patch
Claim boundary: these are seeded local results from the no-model card path. They are not SWE-bench proof, production reliability, whole-program generation speedups, hidden model learning, or evidence that compact cards always beat direct LM work.

Precompute rule for fast patches:

Repo / surface state
-> PatchCardIndex
-> nearest patch-card prior
-> fill missing request-specific fields
-> VM/BVT gate
-> edit only if admitted
Precompute patch-card priors, not code mutations. The cache object should make common work start from a warm control shape:

{
  "intent_signature": "bounded task pattern",
  "route": "selected executable surface",
  "target_files": [],
  "existing_surfaces": [],
  "tests": [],
  "rollback": "how to discard or reverse the candidate",
  "risk_flags": [],
  "claim_boundary": "what this card proves and does not prove",
  "evidence_handles": [],
  "freshness": {
    "file_hashes": {},
    "test_config_hash": "",
    "dependency_hash": "",
    "receipt": ""
  }
}
Invalidate or downgrade a precomputed card when target file hashes, test configuration, dependency lockfiles, capability config, route registry, or receipt freshness changes. If freshness is weak, use the precomputed card only as a hint and re-run /frontdoor/hologram, /hologram/state, /neurosymbolic/run, and /bvt/check before materialization.

Claim boundary: PatchCardIndex accelerates route/file/test/rollback selection. It is not an admitted source patch, not a proof that stale plans are safe, and not permission to bypass VMAdmission or /operator/act.

Generation capability handles:

Answer/patch-card evidence and endpoint list: /Users/jobs/Desktop/growing-api-core/README.md
Product runtime contract: /Users/jobs/Developer/causal-workbench/docs/product-runtime.md
Governed API contract: /Users/jobs/Developer/causal-workbench/docs/minimal-governed-api.md
Latent-state claim boundary: /Users/jobs/Developer/causal-workbench/docs/latent-state-research.md
Claim boundary: typed state transitions enable massive external tool/memory use by turning latent intent into explicit route, packet, lifecycle, proof, and receipt labels. They do not prove hidden model latent-space decoding, model-weight learning, or production autonomy.

Default Graph Write Packet
When a Codex-memory/system/product action is admitted and should become durable memory, emit a graph packet and merge it into canonical memory:

admitted transition
-> graph_packets.json
-> merge_operational_memory.py
-> canonical memory hypergraph
-> read-back verification
-> later-run reuse proof when available
Use existing packet prefixes so the merge operator infers node kinds:

OP:<...> for operation nodes
RECEIPT:run:<...> for receipt nodes
RETRIEVAL:<...> for retrieval state
POLICY:<...> for operating policy
Canonical merge command:

python3 /Users/jobs/Desktop/tmp-meta3-engine-test/scripts/merge_operational_memory.py \
  --current /Users/jobs/Desktop/tmp-meta3-engine-test/runs/memory_hypergraph/current/hypergraph.json \
  --seed /Users/jobs/Desktop/tmp-meta3-engine-test/tmp/unified_hypergraph.json \
  --packets /ABS/PATH/TO/graph_packets.json \
  --report /ABS/PATH/TO/memory_merge_report.json
Current smoke proof:

Packet: /Users/jobs/Desktop/tmp-meta3-engine-test/runs/codex-graph-write/manual-20260510T160106Z/graph_packets.json
Merge report: /Users/jobs/Desktop/tmp-meta3-engine-test/runs/codex-graph-write/manual-20260510T160106Z/memory_merge_report.json
Added: 4 nodes and 3 hyperedges
Canonical count after merge: 1909 nodes / 3558 hyperedges
Read-back nodes include OP:codex:typed_transition_graph_write:20260510T160106Z and RECEIPT:run:codex_graph_write_20260510T160106Z
Claim boundary: graph write and canonical read-back are operational. Later-run default reuse is still only proven when a subsequent scored graph includes the written receipt_run or operation nodes.

Typed Transition Contract
Interpret memory as:

intent -> S -> candidate Δ -> BVT control field C -> admitted S' -> receipt -> next move
Use the smallest transition slice that preserves intent, citations, next action, and claim boundary. If citation/action coverage fails, reopen raw trace evidence.

Search-First Composition
Treat Codex as a search, selection, integration, and verification operator before treating it as a code generator.

Before writing new implementation:

Define the task and target state.
Search local memory, repository surfaces, installed tools, production libraries, APIs, and prior receipts.
Identify the best existing component, capability, packet, route, or engine surface.
Compose it with the thinnest adapter that preserves rollback and receipts.
Verify with tests, live runs, or receipt-backed checks.
Write new code only for the irreducible missing gap.
Claim Boundaries
Compact transition memory is not lossless memory.
Local recall is not global retrieval superiority.
Runtime transition memory is not model-level learned attention.
A product viewer is not production reliability.
If memory is missing or weak, say so and downgrade the claim.
Working Rule
Use the existing kernel execution surface in-memory/live; do not keep expanding repos with written proof files.
No more bespoke files-as-system; use prebuilt components to abstract away.
Act proactively, with only brief insight updates.
Avoid ad hoc Python/x machinery when an existing system surface can do the work.
For BVT/RTK/NStar/Meta3/Meta5/Codex-memory work, do not satisfy "proceed" by writing a new miner, extractor, compiler, classifier, or proof-file generator. First route through recovered ControlState, CAS/WorkManifest packets, promoted surfaces, live MCP/app tools, ledger, exact thread reopen, graph-core/UTIR where execution is actually selected, requirements.lock, existing runners, and receipts. Treat system_frontdoor as a legacy fallback, not the default search path. Add code only after a receipt identifies the tried surface, the expressivity gap, the minimal adapter, rollback, and receipt/memory update path.
When working with edge-scope ML/AI, use Pareto-frontier ML principles, clean product one-liners, matrix grids, and empirical evidence.
